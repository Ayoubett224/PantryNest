import type {MetadataRoute} from "next";import {products} from "@/lib/products";import {store} from "@/lib/store";export default function sitemap():MetadataRoute.Sitemap{const staticRoutes=["","/shop","/about","/contact","/faq","/policies/shipping","/policies/returns","/policies/privacy","/policies/terms"].map(path=>({url:`${store.url}${path}`,lastModified:new Date(),changeFrequency:"weekly" as const,priority:path===""?1:0.7}));const productRoutes=products.map(p=>({url:`${store.url}/product/${p.slug}`,lastModified:new Date(),changeFrequency:"daily" as const,priority:0.9}));return [...staticRoutes,...productRoutes]}
