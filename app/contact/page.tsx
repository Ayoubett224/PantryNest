"use client";import {FormEvent,useState} from "react";import {store} from "@/lib/store";export default function Contact(){const[state,setState]=useState("");async function send(e:FormEvent<HTMLFormElement>){e.preventDefault();setState("Sending…");const form=new FormData(e.currentTarget);const r=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(form.entries()))});setState(r.ok?"Message received. We’ll respond using the contact details you provided.":"Message could not be sent. Please email or call us directly.");if(r.ok)e.currentTarget.reset()}return <section className="section container narrow prose"><p className="eyebrow">Customer support</p><h1>Contact us</h1><p>Email: <a href={`mailto:${store.email}`}>{store.email}</a><br/>Phone: <a href={`tel:${store.phone}`}>{store.phone}</a><br/>Business address: {store.address}</p><form className="contact-form" onSubmit={send}><label>Name<input name="name" required/></label><label>Email<input type="email" name="email" required/></label><label>Order number (optional)<input name="orderNumber"/></label><label>Message<textarea name="message" rows={6} required/></label><button className="button primary">Send message</button>{state&&<p>{state}</p>}</form></section>}
